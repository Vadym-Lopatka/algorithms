package com.vadym.lopatka.algorithms;

import java.util.*;

public class BreadthFirstAlgorithm {

    public static String findClosestWayToFriendsFriend(HashMap<String, HashMap> you, String friendName) {

/*
        LinkedList<HashMap<String, HashMap>> queue = new LinkedList<>();
        queue.add(you);

        HashSet<String> alreadyCheckedFriends = new HashSet<>();


        while (!queue.isEmpty()) {
//            Set<Map.Entry<String, HashMap>> entries = queue.poll().entrySet();

            HashMap<String, HashMap> friendGraph = queue.poll();
//            friendGraph.entrySet().
//            if (alreadyCheckedFriends.contains(friend.keySet()))

            if

        }

*/

        return "";
    }
}
